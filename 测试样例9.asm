DATAS SEGMENT
	;此处输入数据段代码
	NUM DW 0
	CRLF DB 0DH,0AH,"$"  
DATAS ENDS

STACKS SEGMENT
	;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
	ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
	MOV AX,DATAS
	MOV DS,AX
	;此处输入代码段代码
	MOV DX,0
	MOV CX,4
L:
	MOV AH,1
	INT 21H
	CMP AL,40H
	JG L1
	JL L2
L1:
	SUB AL,07H
L2:
	SUB AL,30H
	
	PUSH AX
	MOV AX,NUM
	MOV BX,16
	MUL BX
	POP BX
	ADC AL,BL
	MOV NUM,AX
	LOOP L
	
	LEA DX,CRLF
	MOV AH,9
	INT 21H
	
	MOV CX,16
	MOV BX,NUM
L3:
	MOV DL,31H
	SAL BX,1
	JC L4
	SUB DL,1
L4:
	MOV AH,2
	INT 21H
	LOOP L3
	
	
	
	MOV AH,4CH
	INT 21H
CODES ENDS
	END START